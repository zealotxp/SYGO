# 2026-07-23 工作记录

## v1.3.23 — 药品详情页新增「购药流程」副标题(mock)
- 需求：商品/药品详情页(drug-detail.html)增加副标题，展示购药流程 交订单—登记信息—联系商家—预约取药，暂用 mock 数据。
- 改动：
  - pages_raw/drugDetailPage.html：hero 之后、药品介绍之前新增 `.drug-flow` 块(`.drug-flow-label`="购药流程" + 空 `.drug-flow-steps`#drugFlowSteps)。
  - js/app.js：新增 mock 常量 `DRUG_DETAIL_FLOW = ['交订单','登记信息','联系商家','预约取药']` + `renderDrugDetailFlow()`(按数组生成 4 个带序号圆点的步骤，步骤间用 ::after 虚线连接)；在 `renderDrugDetail()` 末尾调用 `renderDrugDetailFlow()`。
  - css/common.css：新增 `.drug-flow`/`.drug-flow-label`/`.drug-flow-steps`/`.drug-flow-step`/`.drug-flow-dot`/`.drug-flow-name` 样式(白卡、4 等分、圆点+标签、步骤间浅色连线)。
- 重要发现(管线)：本项目 js/app.js 是实际被引用/部署的文件；js/app.raw.js 为过时/独立源(不含 renderDrugDetail)，且 auto-commit.sh 只 commit+push、不 rebuild。故 JS 改动直接改 js/app.js 即可(勿误改 app.raw.js 后期待 build)。assemble.py 只重建根 HTML，不碰 app.js/css。
- 验证：本地无头 Chrome 确认 购药流程 标题 + 4 步(交订单/登记信息/联系商家/预约取药) 渲染；线上 drug-detail.html 200 + X-Cache:MISS + 含 drugFlowSteps，app.js 含 DRUG_DETAIL_FLOW + package.json=1.3.23。
