# 2026-07-23 工作记录

## v1.3.23 — 药品详情页新增「购药流程」副标题(mock)
- 需求：商品/药品详情页(drug-detail.html)增加副标题，展示购药流程 交订单—登记信息—联系商家—预约取药，暂用 mock 数据。
- 改动：
  - pages_raw/drugDetailPage.html：hero 之后、药品介绍之前新增 `.drug-flow` 块(`.drug-flow-label`="购药流程" + 空 `.drug-flow-steps`#drugFlowSteps)。
  - js/app.js：新增 mock 常量 `DRUG_DETAIL_FLOW = ['交订单','登记信息','联系商家','预约取药']` + `renderDrugDetailFlow()`(按数组生成 4 个带序号圆点的步骤，步骤间用 ::after 虚线连接)；在 `renderDrugDetail()` 末尾调用 `renderDrugDetailFlow()`。
  - css/common.css：新增 `.drug-flow`/`.drug-flow-label`/`.drug-flow-steps`/`.drug-flow-step`/`.drug-flow-dot`/`.drug-flow-name` 样式(白卡、4 等分、圆点+标签、步骤间浅色连线)。
- 重要发现(管线)：本项目 js/app.js 是实际被引用/部署的文件；js/app.raw.js 为过时/独立源(不含 renderDrugDetail)，且 auto-commit.sh 只 commit+push、不 rebuild。故 JS 改动直接改 js/app.js 即可(勿误改 app.raw.js 后期待 build)。assemble.py 只重建根 HTML，不碰 app.js/css。
- 验证：本地无头 Chrome 确认 购药流程 标题 + 4 步(交订单/登记信息/联系商家/预约取药) 渲染；线上 drug-detail.html 200 + X-Cache:MISS + 含 drugFlowSteps，app.js 含 DRUG_DETAIL_FLOW + package.json=1.3.23。

## v1.3.24 — 商家订单页手机号后增加拨打电话按钮
- 需求：merchant-orders.html 每条订单的「手机」后面加一个按钮，点击直接拨打电话。
- 改动：
  - js/app.js renderMerchantOrders()：手机 cell 内的 <b class="v"> 后追加 `<a class="m-call-btn" href="tel:手机号" onclick="event.stopPropagation()">`(电话 SVG 图标，复用 merchant-detail 的拨打电话图标路径)。用 tel: 直链，移动端点击即唤起拨号。
  - css/common.css：新增 `.m-call-btn`(flex:none 26x26 圆形、橙底 var(--orange-light)/图标 var(--orange)、:active 反色)，与现有 `.action-btn-call` 配色一致。
- 注：订单列表由 JS 运行时渲染(requireMerchant 校验商家登录)，HTML 容器 #merchantOrderList 不变，故无需改 pages_raw；改动仅在 app.js+css。
- 验证：node --check js/app.js 通过；线上 app.js 含 m-call-btn、css 含 .m-call-btn、merchant-orders.html 200 + X-Cache:MISS + package.json=1.3.24。

## v1.3.25 — 药品详情页副标题改为纯文本(撤销步骤流样式)
- 反馈：用户要求副标题是「文本」，不要带样式(圆点步骤流)。
- 改动：
  - pages_raw/drugDetailPage.html：删除 .drug-flow 块；在 drug-detail-name 之后新增 `<div class="drug-detail-sub" id="drugDetailSub">`(位于 hero 基本信息区，作为名称下方的副标题文本行)。
  - js/app.js：删除 DRUG_DETAIL_FLOW 数组与步骤渲染；改为常量 `DRUG_DETAIL_SUBTITLE='购药流程：交订单 — 登记信息 — 联系商家 — 预约取药'`，renderDrugDetailFlow() 仅把该文本写入 #drugDetailSub.textContent。
  - css/common.css：删除全部 .drug-flow* 样式；新增 `.drug-detail-sub`(仅字号12/浅灰/行高，纯文本无装饰)。
- 验证：无头 Chrome 确认文本「购药流程：交订单 — 登记信息 — 联系商家 — 预约取药」渲染、无 .drug-flow；线上 drug-detail.html 200 + X-Cache:MISS + 含 drugDetailSub、css 无 .drug-flow + package.json=1.3.25。

## v1.3.26 — 药品详情页副标题移至生产厂家下方
- 需求：把副标题从药名下方移到「生产厂家」(drugDetailSpec) 下方。
- 改动：pages_raw/drugDetailPage.html 中 #drugDetailSub 从 drug-detail-name 之后移到 drug-detail-spec-row(生产厂家 · 规格) 之后；JS/CSS 不变(仍写 #drugDetailSub.textContent)。
- 验证：无头 Chrome 确认 DOM 顺序 spec-row → sub → price；线上 drug-detail.html 200 + X-Cache:MISS + package.json=1.3.26。
