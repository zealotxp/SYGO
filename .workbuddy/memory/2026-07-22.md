# 2026-07-22 工作记录

## 上药GO v1.3.15 — 底部「搜索」菜单改为「学术」园地
- 需求：把首页底部 tab 的「搜索」换成「学术」，并完整实现学术文章功能。
- 改动文件：
  - `_split/assemble.py`：TABBAR 模板「搜索」→「学术」(书本图标 + `switchTab('academic')`)；active 字典 `search`→`academic`；`search.html` 的 tabActive 改为 None（搜索页不再高亮任何 tab）；PAGES 新增 `academic.html / academic-detail.html / academic-search.html`。
  - `js/app.js`：新增 `ARTICLE_CATEGORIES`(3级分类树) + `ARTICLES`(12篇示例文章) + 渲染/筛选/搜索/分类级联/PDF预览下载逻辑；`switchTab` 增加 `academic`；DOMContentLoaded 增加 `academic / academic-detail / academic-search` 三个分支。
  - `css/common.css`：追加学术园地全套样式（卡片/分类底部弹层/详情/PDF预览弹层/空状态）。
  - 新增 `pages_raw/academicPage.html`(列表:搜索框+分类chip+列表+级联弹层)、`academicDetailPage.html`(详情+PDF预览弹层)、`academicSearchPage.html`(搜索结果/无结果)。
- 实现要点：
  1. 文章列表：封面emoji+标题+发布时间+分类标签。
  2. 文章搜索：列表页搜索框提交 → `academic-search.html?q=`，匹配标题或摘要；有结果进结果列表，无结果显示「未找到相关文章」+「返回文章列表」按钮。
  3. 3级分类：底部弹层三列级联（L1/L2/L3），可停在任意级确定，默认「全部分类」。
  4. 文章详情：标题/作者/发布时间/摘要/PDF附件；附件支持「预览」(iframe 弹层) 与「下载」(`<a download>`)；正文 PDF 不在平台存，摘要人工填写用于简介+搜索。
- PDF 附件：`_split/gen_pdfs.py` 用 fpdf2 + 系统 `C:/Windows/Fonts/simhei.ttf` 子集化生成 12 个真实中文 PDF（每篇一个，`articles/aN.pdf`，约32KB/个，总计~390KB，适合 GitHub Pages）。
- 验证：无头 Chrome(`C:/Program Files/Google/Chrome/Application/chrome.exe`，注意不是 D: 盘) dump-dom 确认列表12篇、详情含PDF、搜索「白蛋白」命中1篇、无结果显示空状态+返回按钮。
- 发布：auto-commit.sh → v1.3.15 已 commit+push；线上校验 index.html 已显示「学术」tab、academic.html 与 articles/a1.pdf 均 200 + X-Cache:MISS。

## 上药GO v1.3.16 — 学术园地文章列表优化
- 需求：分类栏右侧加「清空筛选」按钮；文章列表去掉封面，只显示 标题+时间+分类+作者。
- 改动：
  - `js/app.js`：`academicCardHTML` 去掉封面(`.academic-cover`)与 `.academic-card-body` 包裹，meta 改为 `date · cat · author`（顺序 时间→分类→作者）；`updateAcademicCatChip` 在 `academicCat` 有值时把 `#academicCatClear` 设为 `display:inline-block`、否则 `none`。
  - `pages_raw/academicPage.html`：分类栏改为 `.academic-cat-scroll`(可横滚) + 右侧 `.academic-cat-clear` 按钮(`onclick=resetAcademicCategory()`，默认 `display:none`)。
  - `css/common.css`：`.academic-cat-bar` 改 flex 两区(滚动区+固定清空按钮)；新增 `.academic-cat-scroll/.academic-cat-clear`；`.academic-card` 去 flex/封面，meta 用 `::after/::before` 圆点分隔 时间·分类·作者。
- 注意：`academicCardHTML` 被列表页与搜索结果页共用，故两页列表均无封面、统一显示四项。
- 验证：无头 Chrome 确认 12 卡片、0 封面、每卡含 author、清空按钮默认隐藏；线上 academic.html 200 + X-Cache:MISS + 含「清空筛选」+ package.json=1.3.16。

## 上药GO v1.3.17 — 底部菜单选中态不统一修复
- 现象：各页底部菜单结构一致，但「我的预约」页的「预约」tab 无选中(高亮)效果，显得与其它页(首页/学术/我的 均高亮)不统一。
- 根因：`_split/assemble.py` 中 TABBAR 模板占位符为 `{appt}`，active 字典键为 `appt`；但 `my-appointments.html` 的 PAGES 项 `tabActive='myAppointments'`。`a['myAppointments']=' active'` 给字典新增了无效键，而 `{appt}` 始终为空 → 预约 tab 永不高亮（Python 对不存在的键赋值不报错，故 assemble 未崩溃）。
- 修复：将 `my-appointments.html` 的 `tabActive` 改为 `'appt'`（与占位符/字典键一致）。其余 hasTab 页 (home/academic/profile) 的 tabActive 均与字典键匹配，无需改。
- 验证：本地 assemble 后 4 个 tab 页分别高亮 首页/学术/预约/我的；线上 my-appointments.html 含 `tab-item active" onclick="goMyAppointments()"`，package.json=1.3.17。
- 经验：assemble.py 的 tabActive 值必须与 TABBAR 占位符键(home/academic/appt/profile)严格一致；新增带 tab 的页时务必核对。
- **v1.3.19 学术与预约之间新增 AI 菜单**：循证医学智能体对话页(谌元芳)。结构：底部菜单 5 个 tab 顺序 = 首页/学术/AI/预约/我的。assemble.py 同步：TABBAR 新增 ai tab、TABBAR 字典新增 'ai' 键、PAGES 注册 ai.html(data-page='ai',tabActive='ai',modals 含 imagePreviewModal)。pages_raw/aiPage.html 含 header(汉堡/谌元芳·循证医学智能体 ▾/客服)、model-switch 弹层、欢迎屏(Hi+机器人+你可以这样问+免责+MedClaw 徽章)、消息流、底部输入栏(图钉/深度检索/textarea/发送)、左侧抽屉(历史对话+新建对话)、隐藏 file input。js/app.js 新增 state(aiConvs/aiCurrentId/aiDeep/aiAttachedImage/aiStreaming)、localStorage 持久化(shangyao_ai_convs_v1)、(基于关键词规则匹配医学 KB：EGFR/三阴/ANCA/高血压糖尿病/儿童 ALL/肝硬化出血/你好/谢谢/深度检索/图片等；fallback 提示补充信息)、escapeAIHTML。CSS 约 90 行新增在 common.css 末尾。dispose 备注：tabbar 现在 5 项，profile/search 等所有带 tab 的页都自动更新。**经验**：临时调试文件(.js)放在 _split/ 下时一定要在 auto-commit 前 rm -f，否则会被带入 commit（如本次 ai_interactive_test.js 误带入），需后续清理提交。
- 验证：无头 Chrome 渲染 ai.html：header/欢迎/6 建议项/免责/MedClaw/输入栏/抽屉 DOM 全部存在，suggest-items 含正确转义引号；六个 tab 页 tab 顺序均 = 首页/学术/AI/预约/我的；线上 ai.html 200+X-Cache:MISS，package.json=1.3.19。后续清理提交(去除误带入的 _split/ai_interactive_test.js)已 push。

## 上药GO v1.3.18 — 我的预约页时间选择器改为手机端日历(与商家订单一致)
- 需求：my-appointments.html 原用两个 PC `<input type=date>`(filterByOrderRange)，改成与 merchant-orders.html 相同的底部弹层日历区间选择器。
- 改动：
  - `pages_raw/myAppointmentsPage.html`：去掉两个 date input + 重置按钮，改为 `#apptOrderTrigger`(触发条, onclick=openApptDatePicker) + 重置按钮(resetApptOrderRange) + 复制 merchant 的 `.m-date-picker` 弹层 DOM(字段 id 全改 `appt` 前缀)。复用 common.css 已有 `.m-date-*` 样式，无需新增 CSS。
  - `js/app.js`：删除 `filterByOrderRange`/`resetOrderDate`；新增 my-appointments 专用日期选择器函数(openApptDatePicker/closeApptDatePicker/setApptDateTarget/shiftApptMonth/renderApptDateGrid/onApptDayTap/updateApptDateFields/confirmApptDateRange/resetApptOrderRange/resetApptDateRange/updateApptOrderTrigger)，逻辑与 merchant 版逐字一致(仅 id/状态变量前缀 appt/currentOrderStart/End)。状态变量 `apPickerStart/End/Target/Month` 加在 currentOrderStart/End 旁。DOMContentLoaded 的 my-appointments 分支末尾调用 updateApptOrderTrigger()。
  - `renderMyAppointments()` 已有按 currentOrderStart/End 过滤的逻辑，confirmApptDateRange 直接写这两个变量 → 过滤无缝衔接。
- 经验：该「手机端日历区间选择器」已出现两处(merchant-orders / my-appointments)，可考虑抽成通用组件(参数化 pickerId/state/回调)，避免第三次复制。
- 验证：本地无头 Chrome 确认 触发条"全部日期"、"type=date"=0、30 条预约渲染、弹层 DOM 存在；线上 my-appointments.html 200 + X-Cache:MISS + 含 apptDatePicker/openApptDatePicker + 0 个 date input + package.json=1.3.18。

## v1.3.20 (2026-07-22)
- AI 对话页输入框布局调整：
  - `pages_raw/aiPage.html`：`.ai-input-row` 改为竖向(column)，`textarea`(aiTextInput) 上移为整行，`.ai-icon-btn`(附件)+`.ai-deep-pill`(深度检索)+`.ai-send-btn`(发送) 收进新加的 `.ai-input-actions` 行，放在输入框下方。
  - `css/common.css`：`.ai-input-row{flex-direction:column}`、新增 `.ai-input-actions{flex;gap:10px}`、`.ai-text-input-wrap` 改为 `width:100
## v1.3.20 (2026-07-22)
- AI 对话页输入框布局调整：
  - pages_raw/aiPage.html：.ai-input-row 改为竖向(column)，textarea(aiTextInput) 上移为整行，.ai-icon-btn(附件)+.ai-deep-pill(深度检索)+.ai-send-btn(发送) 收进新加的 .ai-input-actions 行，放在输入框下方。
  - css/common.css：.ai-input-row{flex-direction:column}、新增 .ai-input-actions{flex;gap:10px}、.ai-text-input-wrap 改为 width:100% 且放大(padding 12/14、min-height 48、字体 16px、圆角12)、.ai-send-btn 加 margin-left:auto 靠右。
  - 效果：有问题尽管问 输入框占满整宽且放大，附件与深度检索在输入框下方一排，发送按钮在最右。
- 验证：本地无头 Chrome 确认 DOM 顺序(textarea 在上、actions 在下)；线上 ai.html 200 + X-Cache:MISS + 含 ai-input-actions + package.json=1.3.20。

## v1.3.21 (2026-07-22)
- AI 对话页输入框默认高度抬到两行：
  - css/common.css：.ai-text-input-wrap textarea min-height 24px->48px(约2行)、.ai-text-input-wrap min-height 48px->76px。
  - pages_raw/aiPage.html：textarea rows="1"->rows="2"。
  - 注：js/app.js onAIInputChange 仍以 Math.min(scrollHeight,96) 控制自动增高上限(约4行)，2行 min-height 被尊重。
- 验证：本地 assemble 后 ai.html 含 rows="2"；线上 ai.html 200 + X-Cache:MISS + 含 rows="2" + package.json=1.3.21。
